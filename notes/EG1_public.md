# EG1 Public Note

Gate: `GB_G1`
Constant: `kappa_major_arc`

This note isolates the coercive response package. The projected response operator is

`E_tau = Pi_resp L_tau Pi_resp`.

The gate closes once the admissible tube carries a uniform positive floor on the projected response sector. The extracted theorem-level constant is `kappa_major_arc > 0`, obtained from the raw formula

`c_major_raw * arc_density_raw - e_major_raw`.

In mainstream terms, this is the local positive-response statement that blocks degeneration of the primary transport mechanism.
